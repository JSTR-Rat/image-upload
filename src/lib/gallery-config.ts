/** Gallery / upload tuning (client + server should import from here). */
export const GALLERY_PAGE_SIZE = 20;

/** Hard cap per file (server enforces independently). */
export const MAX_UPLOAD_BYTES = 8 * 1024 * 1024;

/** Scroll distance before the “back to top” floating control fades in. */
export const FLOAT_NAV_SCROLL_THRESHOLD_PX = 400;

export const WEBP_QUALITY = 0.8;
